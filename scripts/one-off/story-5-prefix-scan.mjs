#!/usr/bin/env node
// Story-5 title-prefix scan — word bank + output path for the sentence-chain round.
//
// Run: node scripts/one-off/story-5-prefix-scan.mjs [group keys...]
// With no keys, scans every group in the bank below.

import { runScan } from "../title-prefix-scan.mjs";

// Leading-word groups. Each regex tests the NORMALIZED title and is anchored at start.
// "How to save a life" normalizes to "how to save a life" -> does NOT match /^to /. Good.
const groups = {
  to: /^to /,
  "not to": /^not to /,
  in: /^in /,
  save: /^save\b/,
  take: /^take\b/,
  give: /^give\b/,
  let: /^let\b/,
  bring: /^bring\b/,
  promise: /^(a |i )?promise\b/,
  "i will": /^i will\b/,
  "i'll": /^i'?ll\b/,
  "he said": /^he said\b/,
  he: /^he /,
  she: /^she /,
  now: /^now /,
  and: /^and /,
  "i'd": /^i'?d\b/,
  for: /^for /,
  kill: /^kill\b/,
  trade: /^trade\b/,
  sell: /^sell\b/,
  death: /^death\b/,
  devil: /^(the )?devil\b/,
  soul: /^(my )?souls?\b/,
  her: /^her /,
  my: /^my /,
  one: /^one /,
  all: /^all /,
  anything: /^anything\b/,
  everything: /^everything\b/,
  forever: /^forever\b/,
  "no one": /^no one\b/,
  steal: /^steal\b/,
  spare: /^spare\b/,
  "i sold": /^i sold\b/,
  sold: /^sold\b/,
  "i gave": /^i gave\b/,
  gave: /^gave\b/,
  "take my": /^take my\b/,
  "give my": /^give my\b/,
  "i would": /^i would\b/,
  "i'm": /^i'?m\b/,
  "now i": /^now i\b/,
  price: /^(the )?price\b/,
  cost: /^(the )?cost\b/,
  hell: /^hell\b/,
  heaven: /^heaven\b/,
  die: /^die\b/,
  "to die": /^to die\b/,
  dying: /^dying\b/,
  "we'll": /^we'?ll\b/,
  we: /^we /,
  till: /^till\b/,
  until: /^until\b/,
  that: /^that /,
  "don't": /^don'?t\b/,
  "i belong": /^i belong\b/,
  yours: /^yours\b/,
  mine: /^mine\b/,
  "the deal": /^(the )?deals?\b/,
  "the night": /^(the )?night\b/,
  burn: /^burn\b/,
  damned: /^damn(ed)?\b/,
  stay: /^stay\b/,
  hold: /^hold\b/,
  wake: /^wake\b/,
  come: /^come\b/,
  wait: /^wait\b/,
  remember: /^remember\b/,
  forget: /^forget\b/,
  forgive: /^forgive\b/,
  breathe: /^breathe\b/,
  live: /^live\b/,
  listen: /^listen\b/,
  look: /^look\b/,
  trust: /^trust\b/,
  believe: /^believe\b/,
  run: /^run\b/,
  close: /^close\b/,
  open: /^open\b/,
  keep: /^keep\b/,
  hear: /^hear\b/,
  feel: /^feel\b/,
  sleep: /^sleep\b/,
  rest: /^rest\b/,
  fight: /^fight\b/,
  hang: /^hang\b/,
  "i need": /^i need\b/,
  "i'm here": /^i'?m here\b/,
  "i know": /^i know\b/,
  "i can": /^i can\b/,
  "i said": /^i said\b/,
  said: /^said\b/,
  "i told": /^i told\b/,
  told: /^told\b/,
  whisper: /^whisper/,
  "i whisper": /^i whisper/,
  pray: /^pray/,
  "i pray": /^i pray/,
  beg: /^beg/,
  "i beg": /^i beg/,
  swore: /^sw(ore|ear)\b/,
  "i swore": /^i sw(ore|ear)\b/,
  promised: /^promised\b/,
  cried: /^crie[ds]\b/,
  "i cried": /^i crie[d]\b/,
  called: /^call(ed)?\b/,
  "i called": /^i call(ed)?\b/,
  spoke: /^sp(oke|eak)\b/,
  turned: /^turn(ed)?\b/,
  "i turned": /^i turn(ed)?\b/,
  "i held": /^i held\b/,
  held: /^held\b/,
  "i kissed": /^i kiss(ed)?\b/,
  kiss: /^kiss/,
  "i looked": /^i look(ed)?\b/,
  "and i": /^and i\b/,
  "and she": /^and she\b/,
  "and he": /^and he\b/,
  "so i": /^so i\b/,
  "then i": /^then i\b/,
};

const want = process.argv.slice(2);
const keys = want.length ? want : Object.keys(groups);
const outPath = "data/analysis/story-5/prefix-hits.txt";

runScan({ groups, keys, outPath });
